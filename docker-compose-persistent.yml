# 根目錄 Docker Compose Persistent 檔案 - 向後兼容
# 實際檔案位於 scripts/docker/docker-compose-persistent.yml

version: "3.8"

# 這個檔案用於向後兼容，實際配置在 scripts/docker/docker-compose-persistent.yml
# 請使用以下命令：
# docker-compose -f scripts/docker/docker-compose-persistent.yml [command]

services:
  # 重定向到實際的配置檔案
  redirect:
    image: alpine:latest
    command: echo "請使用: docker-compose -f scripts/docker/docker-compose-persistent.yml [command]"